using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Rendering;

public class taa_behaviour : MonoBehaviour
{
    public bool _EnableTAA = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //var myMaterial = GetComponent<Renderer>().material;        
        //lastM = GetComponent<Transform>().localToWorldMatrix;
        //myMaterial.SetMatrix("_LastM", lastM);

        //myMaterial.SetMatrix("_CurrentM", lastM);
    }



}
